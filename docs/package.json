{
  "name": "petraplatform-docs",
  "version": "0.1.0",
  "private": true,
  "description": "Documentação visual do petraplatform (estática + Mermaid)",
  "scripts": {
    "start": "serve -s . -l ${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
